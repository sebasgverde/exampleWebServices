- el listar devuelve el json y como ws funciona, pero sin datos reales, 
la clase listarlogica que es la que parsea el sharedirs y lista los
archivos solo devuelve un arreglo quemado, falta que se genere de datos reales
-con el find ocurre exactamente lom mismo, se diferencia del ws listar por medio
del patron, si mando el patron "" es por los quiere listar
-tanto las url de los ws como la carpeta de los archivos de lee de un archivo
xml que es parseado, en el caso de los archivos aun no hace nada con esa
informacion
- en la pagina web se muestra el json crudo, aun falta parsearlo para que
se muestren solo los nombres de los archivos
- se tiene la aplicacion web con los botones que llaman los ws